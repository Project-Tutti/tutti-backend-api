apiVersion: v1
kind: Service
metadata:
  name: main-server
  namespace: tutti
  labels:
    app: main-server
spec:
  type: ClusterIP
  selector:
    app: main-server
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
